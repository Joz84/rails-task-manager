<h1><%= @task.name %></h1>
<p><%= @task.description %></p>
<p>[<%= @task.done ? "x" : " " %>]</p>

<%= link_to "Edit", edit_task_path %>
<%= link_to "Delete", task_path, method: "delete", data: { confirm: "Are you sure SURE?" } %>
<%= link_to "Done", make_task_path(@task.id), method: "post" %>
